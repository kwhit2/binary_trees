#include "binary_trees.h"

/**
* avl_remove - function that removes a node a from an AVL Tree
*
* @root: ?
* @value: value
*
* Return: ?
*/

avl_t *avl_remove(avl_t *root, int value)
{
	void (tree);
	void (value);
	return (0);
}
